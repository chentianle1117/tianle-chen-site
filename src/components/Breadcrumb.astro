---
interface Props {
  category?: string;
  title: string;
}
const { category, title } = Astro.props;
---

<nav
  aria-label="Breadcrumb"
  class="breadcrumb-sticky sticky z-30 border-b border-graphite-700/40 backdrop-blur-sm"
  style="background-color: color-mix(in srgb, var(--bg) 80%, transparent);"
>
  <div class="container-page py-2">
    <ol class="flex items-center gap-2 font-mono uppercase tracking-[0.08em]" style="font-size: var(--step--1);">
      <li>
        <a
          href="/work"
          class="text-secondary hover:text-accent-hi transition-colors duration-200"
        >
          Work
        </a>
      </li>
      {category && (
        <>
          <li class="text-graphite-600" aria-hidden="true">/</li>
          <li>
            <a
              href={`/work?category=${encodeURIComponent(category)}`}
              class="text-secondary hover:text-accent-hi transition-colors duration-200"
            >
              {category}
            </a>
          </li>
        </>
      )}
      <li class="text-graphite-600" aria-hidden="true">/</li>
      <li class="text-primary truncate max-w-xs md:max-w-md" title={title}>
        {title}
      </li>
    </ol>
  </div>
</nav>

<style>
  /* Round-9c: nav header is 52px mobile / 56px desktop. Previous top:65px
     left a visible gap between the sticky header and sticky breadcrumb
     when the user scrolled. Match exactly so the two bars touch. */
  .breadcrumb-sticky { top: 52px; }
  @media (min-width: 768px) {
    .breadcrumb-sticky { top: 56px; }
  }
</style>
