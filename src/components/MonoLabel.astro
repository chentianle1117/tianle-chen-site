---
interface Props {
  as?: string;
  tone?: "primary" | "secondary" | "tertiary";
  class?: string;
}

const { as: Tag = "span", tone = "tertiary", class: className = "" } = Astro.props;

const toneMap: Record<string, string> = {
  primary: "text-[rgb(var(--text-primary))]",
  secondary: "text-[rgb(var(--text-secondary))]",
  tertiary: "text-[rgb(var(--text-mono))]",
};

const toneClass = toneMap[tone] ?? toneMap.tertiary;
---

<Tag
  class:list={[
    "font-mono uppercase tracking-[0.08em]",
    toneClass,
    className,
  ]}
  style="font-size: var(--step--1)"
>
  <slot />
</Tag>
